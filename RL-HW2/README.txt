To run a single algorithm, type:
python main.py --algo [algo name]

To run two algorithms at the same time, type:
python main.py --runAll

To run algorithm with different parameters alpha at the same time, type:
python main.py --runAlpha --algo [algo name]

If you want to render the testing episode, add --render at the end of every command.